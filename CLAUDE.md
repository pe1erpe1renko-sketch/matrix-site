# MATRIX — сайт-калькулятор матрицы судьбы

Прочитай этот файл целиком перед первым изменением кода. Здесь методика расчёта,
тарифы, тексты, механика и решения, которые уже приняты и пересмотру не подлежат.

---

## 0. КАК ОБЩАТЬСЯ С ЗАКАЗЧИКОМ

- Он не программист. Технические вещи объясняй простым языком, без жаргона.
- Он работает через браузер: claude.ai/code → ветка → предпросмотр на хостинге → слияние.
  Значит задачи приходят крупными кусками. Делай задачу целиком, не оставляй
  «доделаю потом» — вмешаться в середине он не сможет.
- После работы коротко перечисли, что проверить глазами на предпросмотре.
- Если задача противоречит этому документу — скажи об этом прямо и объясни почему,
  а не молча выполняй.

---

## 1. ЧТО ЭТО ЗА ПРОЕКТ

Сайт-калькулятор **матрицы судьбы** с монетизацией по подписке.

**Фокус:** только матрица судьбы и то, что считается из даты рождения той же
формулой. Не агрегатор эзотерики. Таро, натальные карты, сонники, квадрат
Пифагора — **не добавлять**, это решение принято осознанно.

**Что уже есть:** движок расчёта (`src/lib/`) и главная страница (`src/pages/Home.jsx`).

**Что дальше:** остальные страницы, переходы между ними, личный кабинет.
Затем подключение бэкенда, оплаты и нейросети — это делает принимающая команда.

---

## 2. МЕТОДИКА РАСЧЁТА — ЗАКРЫТА, НЕ ПЕРЕСМАТРИВАТЬ

Методика восстановлена обратной инженерией по четырём независимым сервисам
(tvoyamatritsa.ru, matricasudbi-kalkulator.ru, acronum.ru, ma-nu.ru) и сверена
на трёх датах — все сошлись до последней цифры.

### Три правила, которые нельзя нарушать

**1. Приведение к Аркану 1–22 идёт СУММОЙ ЦИФР, а не вычитанием 22.**
`30 → 3`, а не `30 → 8`. Функция `toArcana` в `matrixEngine.js` выглядит
неоптимально — цикл, повторяющий суммирование. Это НЕ повод её «улучшать»
через остаток от деления: это замаскированное вычитание, после него вся
матрица разъезжается.

**2. Оси несимметричны.** От W до центра — три промежуточные точки,
от центра до E — две. Выглядит как ошибка, но только так сходится
чакральная таблица у всех четырёх эталонов.

**3. Планетарное предназначение = духовная гармония + социализация.**

### Эталонные даты для проверки

| Дата | Внешние W/N/E/S | Центр | Углы NW/NE/SE/SW | Итог чакр | Предназначения |
|---|---|---|---|---|---|
| 13.07.1998 | 13 / 7 / 9 / 11 | 4 | 20 / 16 / 20 / 6 | 8 / 9 / 17 | 4 / 8 / 12 / 20 |
| 09.04.1992 | 9 / 4 / 21 / 7 | 5 | 13 / 7 / 10 / 16 | 9 / 10 / 10 | 14 / 10 / 6 / 16 |
| 07.06.1998 | 7 / 6 / 9 / 22 | 8 | 13 / 15 / 4 / 11 | 11 / 14 / 16 | 8 / 7 / 15 / 22 |

**Любая правка формул прогоняется через `npm test`.** Если тест падает —
движок сломан, дальше идти нельзя.

### Аркан дня — наша собственная формула

```
аркан дня = число + месяц + год сегодняшней даты + аркан текущего периода жизни
```

Приводится к 1–22. Аркан периода в формуле обязателен: без него аркан дня был бы
одинаковым у всех людей на планете. Реализовано в `dayArcana()` и `buildToday()`.

Проверка: `dayArcana('2026-08-25', 21) === 10`.

**Не путать два поля:**
- `today.arcana` — аркан периода жизни, держится 2,5 года
- `today.dayArcana` — аркан на сегодня, меняется каждые сутки
- `today.tomorrowArcana` — аркан на завтра

---

## 3. ФАЙЛЫ ДВИЖКА — `src/lib/`

| Файл | Назначение |
|---|---|
| `matrixEngine.js` | весь расчёт. Чистые функции, ноль зависимостей, ноль UI |
| `matrixEngine.test.js` | тесты по трём датам + проверки аркана дня. Запуск: `npm test` |
| `contentPositions.js` | 25 разделов, 51 слот, 25 точек октаграммы, привязка к числам |
| `contentLayer.js` | кэш → эталонные тексты → генерация → заглушка |
| `prompts.js` | системный промпт, три типа запросов, версионирование |
| `seedTexts.js` | 5 эталонных текстов, написаны вручную, задают планку качества |

Эти файлы переносить как есть. Не переписывать, не «оптимизировать».
Можно только добавлять новое.

---

## 4. ТАРИФЫ — ЗАФИКСИРОВАНЫ

| | Бесплатно | Разовый | Свой путь | Близкий круг | Без границ |
|---|---|---|---|---|---|
| **Цена** | 0 ₽ | 490 ₽ разово | 590 ₽/мес | 990 ₽/мес | 1790 ₽/мес |
| Дат целиком | — | 1 | 2 | 5 | безлимит |
| Вопросов открыто | 15 из 92 | 92 | 92 | 92 | 92 |
| Аркан дня | сегодня | сегодня | каждый день | каждый день | каждый день |
| Аркан дня в Telegram | — | — | 1 человек | до 3 | до 7 |
| ИИ-наставник | — | — | 5/день | 20/день | без ограничений |
| PDF | — | ✓ | ✓ | ✓ | ✓ |
| Архив прогнозов | — | — | ✓ | ✓ | ✓ |

```js
export const PLAN_LIMITS = {
  free:      { matrices: 0, messages: 0,  telegram: 0, archive: false, pdf: false, label: 'Бесплатно' },
  once:      { matrices: 1, messages: 0,  telegram: 0, archive: false, pdf: true,  label: 'Разовый разбор' },
  path:      { matrices: 2, messages: 5,  telegram: 1, archive: true,  pdf: true,  label: 'Свой путь' },
  circle:    { matrices: 5, messages: 20, telegram: 3, archive: true,  pdf: true,  label: 'Близкий круг' },
  unlimited: { matrices: Infinity, messages: Infinity, telegram: 7, archive: true, pdf: true, label: 'Без границ' },
};
```

Все лимиты берутся отсюда. Зашитых чисел в компонентах быть не должно.

**Логика цен.** Разовый и первая подписка почти равны намеренно: 100 ₽ разницы
против второй матрицы, аркана дня, наставника и архива. Скачок между 590 и 990
покупает прогнозы близким в Telegram. **Архив прогнозов остаётся после отмены
подписки** — отдать накопленное дешевле, чем потерять человека.

---

## 5. КОНТЕНТ

**Модель:** генерация ИИ на лету с кэшем. Заранее ничего не пишем.

**Как работает:** ключ = `{slotId}_{аркан}`. Смотрим в базу → нашли, отдаём
бесплатно; не нашли → генерируем, **сохраняем**, отдаём. Вариантов конечное
число (51 слот × 22 аркана = 1122), поэтому расходы падают к нулю за первые недели.

**Три защиты в коде:** блокировка от двойной генерации одного ключа, запись
в базу только после успеха, версионирование промпта.

**Аркан дня кэшируется иначе** — ключ включает дату: `day_10_21_2026-08-25`.
Без даты человек через три недели получил бы текст, который уже читал.
В конкретный день дата у всех одна, поэтому разных сочетаний максимум 22 —
не больше 22 генераций в сутки на весь сервис, сколько бы ни было пользователей.
Имя человека в промпт **не** подставляется: текст пишется на числа и общий
для всех с таким сочетанием. Имя вставляется шаблоном при выводе.

**Режим макета:** `CONTENT_MODE = 'stub'` в `contentLayer.js`. Числа считаются
по-настоящему всегда. Переключение на `'live'` + ключ провайдера = рабочий режим.

**Рамки в промптах:** без предсказаний событий, сроков и сумм; без медицинских,
психиатрических, юридических утверждений; про здоровье только как зоны внимания,
без отговаривания от врача. В промпте наставника отдельно: при признаках тяжёлого
состояния не уходить в трактовки арканов, говорить по-человечески и мягко выводить
на живого специалиста.

**Имя и пол.** На числа не влияют, формула одна. Имя подписывает разбор в кабинете,
стоит в приветственной строке аркана дня и в PDF. Пол используется в детской
матрице и в обращениях. **Тексты по полу не различаются** — иначе база удвоится
с 1122 до 2244 без пользы.

---

## 6. КАРТА САЙТА

### Десять типов разбора

Тип разбора — не отдельный расчёт, а фильтр: какие из 92 вопросов показать.
Числа считает один и тот же движок, поэтому десять типов стоят столько же,
сколько один. Список и порядок (по спросу) — в `CALC_TYPES`.

```
1  /matrica/{дата}                Матрица судьбы — все 92 вопроса
2  /sovmestimost/{дата}/{дата}    Совместимость
3  /dengi/{дата}                  Деньги
4  /mama-rebenok/{дата}/{дата}    Мама и ребёнок
5  /detskaya/{дата}               Детская матрица
6  /karma/{дата}                  Карма
7  /zdorovie/{дата}               Здоровье
8  /rod/{дата}                    Род
9  /prednaznachenie/{дата}        Предназначение
10 /biznes/{дата}/{дата}          Бизнес-совместимость

/prognoz/{дата}                   Прогноз — не разбор, а ежедневный возврат
```

Старый `/finansy` переадресуется на `/dengi` навсегда — ссылки на него разошлись.

### Меню слева — одно и то же до и после входа, сворачиваемое

Одиннадцать пунктов в столбец не помещаются, поэтому в меню шесть главных,
остальные открываются из карусели в форме и из блока «кого посмотрим дальше».

```
Матрица судьбы        /matrica
Совместимость         /sovmestimost
Деньги                /dengi
Детская               /detskaya
Прогноз               /prognoz
Род                   /rod
─────────────
ИИ-наставник          /chat
Тарифы и оплата       /tarify
Профиль               /profil
```

### Адреса с датой — главное преимущество проекта

У каждого расчёта свой постоянный адрес:

```
/matrica/13-07-1998
/detskaya/02-11-2019
/sovmestimost/13-07-1998/09-04-1992
/biznes/13-07-1998/09-04-1992
```

Зачем: поисковики индексируют такие страницы, и сайт получает трафик без затрат
на рекламу. Ни у одного из пяти конкурентов этого нет. Плюс ссылку можно переслать
человеку, и он увидит готовый расчёт, а не пустую форму.

**Следствие:** страница результата должна работать по прямой ссылке, без
предварительного заполнения формы. Дата берётся из адреса.

### Поведение главной

Сверху форма расчёта с шестью вкладками, под ней информационные блоки, которые
меняются вместе с вкладкой. Когда человек ввёл дату и перешёл к разбору,
маркетинговый текст исчезает, на его месте — результат.

---

## 7. ЧТО ДЕЛАТЬ ДАЛЬШЕ

Задачи по порядку. Каждая — отдельный заход.

### Задача 1. Маршрутизация и общий макет

- Вынести боковое меню из `Home.jsx` в общий компонент макета
- Подключить `react-router-dom` (уже в зависимостях), завести маршруты из раздела 6
- Меню переключает страницы, а не вкладки формы
- Вкладки формы остаются на главной и меняют адрес: `/matrica`, `/finansy` и т.д.
- Дизайн-токены (цвета, скругления, шрифты) вынести в отдельный файл и
  использовать везде — сейчас они внутри `Home.jsx`

### Задача 2. Страница результата расчёта

- Адрес `/matrica/13-07-1998`, работает по прямой ссылке
- Подключить `calculateMatrix()` из `src/lib/matrixEngine.js`
- Октаграмма: взять компонент `OctaDemo` из `Home.jsx` и подставить реальные числа
  вместо демонстрационных. Клик по точке открывает панель с трактовкой из
  `contentLayer.getText()`. Кнопка «Подробнее» ведёт в раздел по
  `findSectionForPoint()`
- Чакральная таблица, предназначения, родовые программы
- 25 разделов: 4 открыты, 21 под замком. Данные из `buildSectionData()`
- Аркан дня: `today.dayArcana` крупно, под ним «Завтра — аркан N».
  **Не** `today.arcana` — это аркан периода
- DEV-переключатель тарифа для проверки доступов

### Задача 3. Остальные калькуляторы

Совместимость, бизнес-совместимость, детская, прогноз. Совместимость и бизнес
считаются по двум датам одной формулой, отличаются только тексты разделов.

### Задача 4. Личный кабинет

- **Мои матрицы** — список сохранённых людей: имя, дата, пол. Добавление,
  редактирование, удаление. Лимит по тарифу
- У каждой карточки переключатель «Присылать аркан дня в Telegram» и уникальная
  ссылка на бота вида `t.me/matrix_bot?start=a7f3k9`. Статус: не активирована /
  подключено. Кнопка «Отозвать» гасит старый код и выдаёт новый
- Первый, кто нажал «Старт» по ссылке, занимает её; остальным бот отвечает,
  что ссылка уже используется
- Профиль, тарифы и оплата, ИИ-наставник со счётчиком сообщений
- Модалка входа: две вкладки, соцсети (Telegram, MAX, Яндекс, VK), email+пароль,
  чекбоксы согласий

### Задача 5. Точки подключения бэкенда

Файлы-заглушки с понятными интерфейсами, чтобы принимающая команда просто
вставила реализацию: `payment.js`, `auth.js`, `chat.js`, `pdf.js`, `telegram.js`.

### Задача 6. README для принимающей команды

Где движок, как устроен словарь позиций, где точки подключения, как переключить
`CONTENT_MODE` на `live`, как запускать тесты, схема таблиц Supabase.

---

## 8. ДИЗАЙН

Тёмный сине-фиолетовый фон, золото на главных действиях, сиреневый на
второстепенных, серифные заголовки с золотым курсивом.

```
фон              #0A0817
фон секции       #0F0B20
полоса-акцент    #161031
карточка         rgba(23,18,46,0.72)
рамка            #2C2552
рамка активная   #4A3D85
золото           #E4BE72
золото ярче      #F3D69C
сиреневый        #B79CE8
кнопка сиреневая #A78BDF
текст            #BDB6D8
текст тусклый    #847CA2
белый            #F5F2FC
тёмный на светлом #140F2B
```

Скругления: 24 крупные блоки, 20 карточки, 14 поля и кнопки, полукруг у вкладок.
Заголовки — Playfair Display, текст — Inter.

**Живое оформление:** звёздное поле в три слоя с параллаксом за курсором,
воронка из девяти вложенных октаграмм по центру экрана, кастомный курсор
(золотая точка в сиреневом кольце, кольцо сжимается при нажатии).
На тач-устройствах курсор и параллакс отключаются.

**Важно:** активные состояния задаются во встроенных стилях, а не через CSS-классы.
Встроенные стили перебивают таблицу стилей — если задать подсветку классом,
она не сработает. Это уже была одна ошибка, не повторяй.

---

## 9. ОТКРЫТЫЕ ВОПРОСЫ

- **Телеграм-бот** — отдельный продукт, прорабатывается позже. Пока заглушка
- **Иллюстрации арканов** — нужны свои от дизайнера либо колода со свободными
  правами. Колода Райдера-Уэйта издания U.S. Games — чужая интеллектуальная
  собственность, использовать нельзя
- **Логотип** — сейчас текстовый MATRIX со звездой. Делает дизайнер
- **Отзывы** — на сайте нет и не будет, пока не появятся настоящие. Выдуманные
  отзывы не ставить
- **22 страницы арканов** под поисковый трафик — отложены, вернёмся позже
- **Годовая оплата со скидкой ~30%** — обсуждалась, отложена

---

## 10. КОНКУРЕНТЫ

| Сайт | Что взято | Чего у них нет |
|---|---|---|
| tvoyamatritsa.ru | **эталон расчёта**, набор калькуляторов | подписки с автопродлением |
| matricasudbi-kalkulator.ru | структура разделов, заголовки-боли | даты в адресе страницы |
| acronum.ru | интерфейс приложения, слои октаграммы, звук, курсор | ежедневного возврата |
| ma-nu.ru | буквенные коды точек | фокуса: 200 страниц без продукта |
| iiset.io | «считает, а не сочиняет» | привязки к периоду жизни |

**Где обходим всех:** рекуррентная подписка, аркан дня привязан к периоду жизни
конкретного человека, доставка близким в Telegram, дата в адресе страницы,
работающая мобильная версия.

Отдельно к сведению: у лидера рынка основная выручка не с калькулятора, а с
обучения (курсы 5990–27990 ₽) и франшизы. Это резерв для руководства, не задача.

/**
 * ДИЗАЙН-ТОКЕНЫ
 * =============
 * Единственный источник правды по цветам, скруглениям и шрифтам.
 * Значения зафиксированы в CLAUDE.md, раздел «Дизайн» — менять их можно
 * только там же, вместе с документом.
 *
 * Правило из CLAUDE.md: активные состояния (подсветка выбранной вкладки,
 * пункта меню, кнопки) задаются ВСТРОЕННЫМИ стилями. Встроенный стиль
 * перебивает таблицу стилей, поэтому классом такую подсветку не сделать.
 * Классы из globalCss.js остаются только для :hover и :focus.
 */

/** Цвета. */
export const C = {
  bg: "#0A0817",        // фон страницы
  bgAlt: "#0F0B20",     // фон секции
  band: "#161031",      // полоса-акцент
  cardHi: "#1F1841",    // подсвеченная карточка
  border: "#2C2552",    // рамка
  borderHi: "#4A3D85",  // рамка активная
  gold: "#E4BE72",      // золото — главные действия
  goldHi: "#F3D69C",    // золото ярче — наведение
  lilac: "#B79CE8",     // сиреневый — второстепенное
  lilacBtn: "#A78BDF",  // кнопка сиреневая
  text: "#BDB6D8",      // основной текст
  muted: "#847CA2",     // текст тусклый
  white: "#F5F2FC",     // белый
  ink: "#140F2B",       // тёмный на светлом фоне

  // Служебные оттенки: в таблице CLAUDE.md их нет, но по коду они
  // повторялись, и каждый раз чуть по-своему. Держим здесь.
  pink: "#E68AB0",      // третий цвет графиков: эмоции, линия отношений.
                        // Он же предупреждение: лимит исчерпан, удаление аккаунта
  ok: "#5FC08C",        // подтверждение: подключено, оплачено, привязано
  navHover: "#191331",  // подложка пункта меню под курсором
  disabled: "#2A2350",  // фон неактивной кнопки
  faint: "#6E6690",     // текст неактивной кнопки и подсказка в поле
  star: "#EDE6FF",      // звёзды на фоне
};

/** Скругления. */
export const R = {
  xl: 24,    // крупные блоки
  lg: 20,    // карточки
  md: 14,    // поля и кнопки
  sm: 11,    // мелкие элементы
  pill: 999, // полукруг у вкладок
};

/** Шрифты: серифные заголовки, гротеск в тексте. */
export const FONT = {
  serif: "'Playfair Display', Georgia, serif",
  sans: "'Inter', -apple-system, 'Segoe UI', Roboto, sans-serif",
};

/**
 * Полупрозрачные подложки карточек. Раньше эти же значения были рассыпаны
 * по компонентам и успели разойтись в третьем знаке прозрачности —
 * теперь берутся отсюда.
 */
export const SURFACE = {
  card: "rgba(23,18,46,0.72)",    // обычная карточка
  cardHi: "rgba(31,24,65,0.86)",  // раскрытая или выделенная карточка
  /* Тот же цвет карточки, но непрозрачный. Нужен липким ячейкам таблицы:
     полупрозрачный фон при прокрутке просвечивал бы насквозь. */
  cardSolid: "#15112B",
};

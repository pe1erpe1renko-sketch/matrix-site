import React from "react";
import Home from "./pages/Home.jsx";

/**
 * ВРЕМЕННОЕ СОСТОЯНИЕ
 * ===================
 * Сейчас приложение состоит из одной страницы — главной.
 * Меню слева внутри Home.jsx переключает вкладки формы, но НЕ переходит
 * между страницами: маршрутизации пока нет.
 *
 * ПЕРВАЯ ЗАДАЧА (см. CLAUDE.md, раздел «Что делать дальше»):
 * разнести проект на маршруты, вынести боковое меню в общий макет
 * и подключить движок расчёта из src/lib.
 *
 * react-router-dom уже стоит в зависимостях — устанавливать ничего не нужно.
 */
export default function App() {
  return <Home />;
}
